/**
 * Re-export fav_star from its canonical location.
 * Import from here when working within the components/ directory.
 */
export {
	loadMyFavouritesSet,
	favStarHtml,
	setFavStarEl,
	setFavStarsForSku,
	installFavStars,
} from "../fav_star.js";
